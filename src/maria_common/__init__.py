"""Código compartido entre `maria` (servidor) y `maria_render` (notebook).

Sin dependencias de red (`httpx`) ni de navegador (`playwright`): solo parsing
puro, modelos pydantic y el contrato del render bundle.
"""
